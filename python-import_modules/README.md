# Python - Import & Modules

This directory contains Python scripts that explore importing functions and modules.

## Files

* `0-add.py` - Imports the function `add(a, b)` from `add_0.py` and prints the result of `1 + 2 = 3`
